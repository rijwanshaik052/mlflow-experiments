name: mlflow-example

conda_env: environment.yml   # or you can use docker_env

entry_points:
  main:
    parameters:
      alpha: {type: float, default: 0.5}
      l1_ratio: {type: float, default: 0.1}
    command: "python mljob.py --alpha {alpha} --l1_ratio {l1_ratio}"
